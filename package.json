{
  "name": "camerashuttercount",
  "private": true,
  "scripts": {
    "dev": "bun run --hot ./src/index.html",
    "build": "bun build ./src/index.html --minify --target browser --outdir ./dist && cp -r assets dist/"
  },
  "dependencies": {
    "preact": "^10.28.3",
    "simpledotcss": "^2.3.7"
  },
  "devDependencies": {
    "@types/w3c-web-usb": "^1.0.13"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
